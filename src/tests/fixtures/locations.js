const locations = {
  home: { pathname: `${__PATH_PREFIX__}/` },
  projects: { pathname: `${__PATH_PREFIX__}/projects` },
  about: { pathname: `${__PATH_PREFIX__}/about` },
}

export default locations
